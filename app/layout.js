import './globals.css'

export const metadata = {
  title: 'AURELIS GTM — Autonomous Growth Intelligence',
  description: 'Budget-aware AI GTM operations with agent-native USDC payments.'
}

export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>
}